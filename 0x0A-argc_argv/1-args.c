#include <stdio.h>

/**
 * main - Entry point.
 * @argc: no. of arg.
 * @argv: arg. array.
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	/* body */
	printf("%d\n", argc - 1);
	return (0);
}

